# 2026-06-09 开发日志

## 今日目标
- [x] 域名 checkmemory.cn 绑定 Vercel 上线
- [x] 方案设计：AI 分段提取解决长文档提取不完整
- [x] 实现 API 路由：智能分块、逐段处理、合并去重
- [x] 修改前端：进度提示和部分成功提示
- [x] 编译验证通过

## 完成内容
### 域名上线
- DNSPod 配置 A 记录 @ → 76.76.21.21
- DNSPod 配置 CNAME www → checkmemory-cn.vercel.app
- Vercel 绑定 checkmemory.cn 成功，SSL 证书自动签发
- https://checkmemory.cn 可正常访问

### AI 分段提取（Chunking）
**修改文件：**
- `src/app/api/extract/route.ts` — 核心逻辑重写
  - 新增 `chunkText()` 智能分块函数
  - 新增 `callDeepSeek()` 含重试机制
  - 新增 `mergeQuestions()` 合并去重
  - 文本 > 25,000 字符自动分块，段间重叠 2,000 字符
  - 边界优先在段落 \n\n 处切分
  - 逐段顺序调用，单块失败不影响其他块
  - 多段只计 1 次配额
  - 部分成功返回 `{ partial: true, message }`

- `src/components/FileUpload.tsx` — 前端优化
  - 进度提示增加等待说明
  - 错误信息更具体（显示 API 返回的错误描述）
  - 部分提取成功时显示警告 toast

### 编译验证
- `npm run build` ✅ 编译成功，无 TypeScript 错误

### AI 提取成功率提升（JSON 容错 + Prompt 优化）
**修改文件：** `src/app/api/extract/route.ts`

**方案 A — JSON 容错恢复：**
- 新增 `tryExtractJSON()` 函数，5 步递进恢复策略
- 第1步：直接 JSON.parse
- 第2步：去掉 Markdown 代码块标记后重试
- 第3步：正则提取 JSON 对象
- 第4步：正则提取 JSON 数组
- 第5步：修复常见错误（末尾逗号、未转义换行、单引号）
- 全部失败才返回 null，走重试逻辑

**方案 B — Prompt 优化：**
- 从 7 条强制要求精简为 3 条简洁规则
- 增加 Few-shot 示例（二叉树问答示例）
- 开头结尾都强调 JSON 输出格式
- 去掉模糊指令（"逐字逐句"、"不要做价值判断"）

### 账号限制调整
- **每日提取限制**：普通用户从 10 次/天 → **3 次/天**
- **文件大小限制**：新增 20MB 上限（前端 + 服务端双重校验）
- 错误提示从"额度"改为"提取次数"

### 界面显示剩余次数
- 新增 `/api/usage` 接口返回当前用户使用量
- 上传区域下方显示：管理员 👑 不限 / 普通用户 📊 今日剩余 X/3 次 + 📁 文件最大 20MB
- 管理员不限制文件大小和提取次数
- 首次加载时自动获取使用量

### 自定义邮箱验证（QQ邮箱 SMTP）
**背景：** Supabase 验证邮件链接指向 `supabase.co`，国内无法访问
**解决：** 配置 QQ 邮箱 SMTP 自定义发件，验证链接指向 `checkmemory.cn`

**Supabase 后台配置（通过 API）：**
- SMTP：smtp.qq.com:465，发件人"记忆助手"
- Site URL：https://checkmemory.cn
- 回调白名单：checkmemory.cn + localhost
- 邮件模板：中文标题 + 内容 + 样式化按钮

**代码修改：**
- 新增 `src/app/auth/callback/route.ts` — 验证回调，交换 code → session → 跳转首页
- 修改 `src/app/auth/register/page.tsx` — 注册提示明确告知检查 QQ 邮箱

## 下一步
- [ ] 测试：注册新账号 → 收验证邮件 → 点击验证 → 登录
